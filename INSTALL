# INSTALLATION

# (Assuming a Ubuntu machine.)

# Install required software:

> sudo apt-get install git lua5.1 lua-lpeg valgrind

# Clone Céu repository:

> git clone https://github.com/fsantanna/ceu
> cd ceu/

# Run self tests:

> ./run_tests.lua           # takes a long time (w/ valgrind)
                            # OR
> ./run_tests.lua false     # much faster (w/o valgrind)

# Include Céu in your path:

> echo 'export PATH="$PATH:/.../ceu/"' >> ~/.bashrc
